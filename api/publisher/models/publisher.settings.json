{
  "kind": "collectionType",
  "collectionName": "publishers",
  "info": {
    "name": "publisher"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "uid",
      "required": true
    },
    "games": {
      "via": "publisher",
      "collection": "game"
    }
  }
}
